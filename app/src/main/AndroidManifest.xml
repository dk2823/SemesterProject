<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dk.semesterproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterActivity"
            android:label="Register"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".OrderActivity"
            android:label="Order"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".PasswordRecoveryActivity"
            android:label="Password Recovery"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".OrderPickupActivity"
            android:screenOrientation="portrait"
            android:label="Working On Order"></activity>
        <activity
            android:name=".OrderConfirmActivity"
            android:label="Confirm page"
            android:screenOrientation="portrait"></activity>

    </application>

</manifest>